<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>WQ小视频管理后台</title>
</head>
<link href="https://unpkg.com/semantic-ui@2.3.3/dist/semantic.min.css" rel="stylesheet">
<script src="https://unpkg.com/jquery@3.3.1/dist/jquery.min.js"></script>
<script src="https://unpkg.com/semantic-ui@2.3.3/dist/semantic.min.js"></script>
<body>
<div style="margin-top: 100px;">
    <div class="ui raised very padded text container segment">
        <form class="ui form" method="post" action="/login">
            <div class="field">
                <label>姓名</label>
                <input type="text" name="username" placeholder="用户名">
            </div>
            <div class="field">
                <label>密码</label>
                <input type="password" name="password" placeholder="密码">
            </div>
            <button class="fluid ui blue button" type="submit">登录</button>
            <div class="field">
                <label style="margin-top: 20px; text-align: center;">* 如您未注册，系统将自动为你注册</label>
            </div>
        </form>
    </div>
</div>
<script>
  $(function () {
    $('.ui.form')
      .form({
        inline: true,
        on: 'blur',
        fields: {
          username: {
            identifier: 'username',
            rules: [
              {
                type: 'empty',
                prompt: '请输入用户名'
              }
            ]
          },
          password: {
            identifier: 'password',
            rules: [
              {
                type: 'empty',
                prompt: '请输入密码'
              }
            ]
          }
        }
      });
  })
</script>
<% include footer %>